# CSPC AI Platform (Next.js)

This project is a safe port of the original CSPC Streamlit AI Search application to Next.js (React).
All secrets remain server-side and are never exposed to the browser.

## Install

```bash
npm install
